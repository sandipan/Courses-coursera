sigma <- function(z) {
  return(1/(1+exp(-z)))
}

beta_0 <- 1
beta_1 <- 1
x <- -1.5
z <- beta_0 + beta_1*x

sigma(z)

1/(1+exp(-1/2))

1/(1+exp(-(0.5-2.0+2.0)))
1/(1+exp(-(0.5-2.0-1.0)))
1/(1+exp(-(0.5-1.0+2.0)))
1/(1+exp(-(0.5+1.0-2.0)))

49+20+32
49+20+32+1+2+14+14